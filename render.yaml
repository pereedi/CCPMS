services:
  - type: web
    name: mission-control-system
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        value: file:./dev.db
      - key: JWT_SECRET
        value: ccpms_production_secret_jwt_key_2026_antigravity
      - key: NODE_ENV
        value: production
